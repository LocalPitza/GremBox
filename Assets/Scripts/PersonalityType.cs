using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum PersonalityType
{
    Playful,    // Seeks attention, frequent play needed
    Glutton,    // Gets hungry faster, eats more
    Lazy,       // Moves slower, hates being disturbed
    Curious,    // Explores more, reacts to everything
    Shy         // Avoids others, hides when needs are low
}
